import styled from "styled-components";

const Container = styled.div`
  display: flex;
  justify-content:space-between;
  flex-wrap: wrap;
  padding:var(--padding);
  gap:20px;
//   max-width:1440px;
`;

export { Container };
